import React from 'react'
import Container from '../../shared/container'
import HeroBanner from '../../shared/hero-banner'

const Hero = ({ data, isProfile = true }) => {
    // console.log(data)
    return (
        <Container
        parentStyle={`md:!h-[21vh] !h-[15vh] bg-darkBlue relative `}
            className={'lg:px-16 sm:px-8 px-4'}
        >
            <></>
        </Container>
    )
}

export default Hero
